// import random from "./random";

// const audioCtx = new window.AudioContext();

export default function beep() {
  // const osc = audioCtx.createOscillator();
  // const gain = audioCtx.createGain();
  // osc.type = "triangle"; // retro sound
  // osc.frequency.setValueAtTime(random(100, 400), audioCtx.currentTime);
  // osc.frequency.exponentialRampToValueAtTime(500, audioCtx.currentTime + 0.1);
  // gain.gain.setValueAtTime(random(0.1, 1), audioCtx.currentTime);
  // gain.gain.exponentialRampToValueAtTime(0.001, audioCtx.currentTime + 0.2);
  // gain.gain.setValueAtTime(random(100, 200), audioCtx.currentTime);
  // gain.gain.exponentialRampToValueAtTime(0.001, audioCtx.currentTime + 0.3);
  // gain.gain.value = 0.1;
  // osc.connect(gain);
  // gain.connect(audioCtx.destination);
  // osc.start();
  // osc.stop(audioCtx.currentTime + 0.3);
}
